import { NgxBalanceTextDirective } from './ngx-balance-text.directive';

describe('NgxBalanceTextDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxBalanceTextDirective();
    expect(directive).toBeTruthy();
  });
});
